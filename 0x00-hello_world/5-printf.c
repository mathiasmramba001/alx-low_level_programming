#include <stdio.h>

/**
 * main - Entry point - output a string of arrays in a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
